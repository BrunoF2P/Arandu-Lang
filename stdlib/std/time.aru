module std.time

/// **Experimental — not Arandu Minimal 0.1.**
/// Thin SL_S: clocks (scaffold).

extern "C" {
    /// Monotonic time in nanoseconds since an arbitrary epoch.
    func monotonic_ns(): int
}

module std.fs

/// **Experimental — not Arandu Minimal 0.1.**
/// Thin SL_S: filesystem (scaffold — declarations only).

extern "C" {
    /// Returns true if `path` exists (host ABI TBD).
    func exists(path: str): bool
}

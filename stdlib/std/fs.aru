module std.fs

/// experimental — not Minimal 0.1
///
/// Thin SL_S filesystem scaffold (declarations only; host ABI not Minimal-complete).
/// Do not use from `arandu new` templates. Track: PROMOTE when hosts land.

extern "C" {
    /// Returns true if `path` exists (host ABI TBD).
    func exists(path: str): bool
}

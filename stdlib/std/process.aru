module std.process

/// **Experimental — not Arandu Minimal 0.1.**
/// Thin SL_S: process control (scaffold).

extern "C" {
    /// Terminate the process with the given status code.
    func exit(code: int): void
}

module examples.stable.syntax.structs

import io

public struct User {
    public name: str
    age: int
}

func User.greet(user: User) : str {
    return "Ola, ${user.name}"
}

func main() {
    let user = User { name: "Ana", age: 30 }
    io.println(User.greet(user))
}
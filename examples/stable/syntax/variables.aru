module examples.stable.syntax.variables

import io

func main() {
    name: str = "Bruno"
    mut age: int = 25
    score = 1_000
    ratio = 3.14
    active = true

    set age = age + 1
    set score += 50

    io.println("${name}: ${age}, ${score}, ${ratio}, ${active}")
}

func test_alloc_free() {
    let buffer = alloc(64)
    free(buffer)
}

func test_defer(): int {
    let mut x = 0
    defer {
        x = 1
    }
    return x
}

func test_defer_scope(): int {
    let mut x = 0
    if true {
        defer {
            x = x + 10
        }
        x = 5
    }
    return x
}

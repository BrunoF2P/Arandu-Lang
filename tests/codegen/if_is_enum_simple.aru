enum LoadState {
    Idle,
    Loading,
    Success,
    Failure,
}

func test_enum(state: LoadState) int {
    mut res = 0
    if state is LoadState.Idle {
        set res = 1
    } else if state is LoadState.Loading {
        set res = 2
    } else {
        set res = 3
    }
    return res
}

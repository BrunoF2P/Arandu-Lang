enum Result {
    Ok(int),
    Error(str),
}

func test_payload(res Result) int {
    mut val = 0
    if res is Result.Ok(x) {
        set val = x
    } else {
        set val = -1
    }
    return val
}

enum Result {
    Ok(int),
    Error(str),
}

func test_payload(res: Result): int {
    let mut val = 0
    if res is Result.Ok(x) {
        val = x
    } else {
        val = -1
    }
    return val
}

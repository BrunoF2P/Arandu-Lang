module examples.stable.syntax.variables

import io

func main() {
    let name: str = "Bruno"
    let mut age: int = 25
    let score = 1_000
    let ratio = 3.14
    let active = true

    age = age + 1
    score += 50

    io.println("${name}: ${age}, ${score}, ${ratio}, ${active}")
}

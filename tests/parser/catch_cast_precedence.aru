module tests.catchCastPrecedence

func main() {
    a = read() catch 0
    b = read() catch |err| {
        io.println(err)
        return 0
    }
    c = value as i32 + 1 * 2
    d = left ?? right catch fallback
}

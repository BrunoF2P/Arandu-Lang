module tests.catchCastPrecedence

func main() {
    let a = read() catch 0
    let b = read() catch |err| {
        io.println(err)
        return 0
    }
    let c = value as i32 + 1 * 2
    let d = left ?? right catch fallback
}

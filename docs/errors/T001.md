# Error T001: Cannot Infer Type

This error occurs when the type checker cannot determine the type of a variable without a type annotation.

## Incorrect Code Example

```arandu
func main() {
    x = nil
}
```

## Semantic Explanation

Arandu uses type inference, but some expressions (like a bare `nil` or uninitialized variables) do not provide enough type context.

## How to Fix

Add an explicit type annotation:

```arandu
func main() {
    x: ptr[int] = nil
}
```
